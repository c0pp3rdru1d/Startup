[project]
name = "sentineldesk"
version = "0.1.0"
description = "Local-first network ops dashboard (PySide6 + SQLite)"
requires-python = ">=3.11"
dependencies = [
  "PySide6>=6.6",
  "sqlmodel>=0.0.21",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

